import AppCounter from '../components/AppCounter'

const Stats = () => {
    return (
        <div className="flex justify-center">
            <AppCounter />
        </div>
    );
};

export default Stats;